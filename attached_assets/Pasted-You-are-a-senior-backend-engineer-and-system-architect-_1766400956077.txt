You are a senior backend engineer and system architect.

Refactor and UPGRADE the existing GymBuddy AI project in this Replit.
KEEP the existing frontend UI and pages.
Your task is to make the BACKEND real, persistent, secure, and scalable.

This project is for PLACEMENTS and FUTURE PRODUCT USE.
No shortcuts, no fake logic, no in-memory demo data.

=============================
TECH STACK (STRICT)
=============================
- Backend: Node.js + Express
- Database: SQLite (persistent, file-based)
- ORM: Prisma
- Authentication: JWT
- Passwords: bcrypt hashing
- Frontend: EXISTING UI (do not redesign)
- Hosting: Replit (demo only)

=============================
MANDATORY BACKEND REQUIREMENTS
=============================

1) REAL DATABASE (NO IN-MEMORY DATA)
- Use SQLite with Prisma
- Create proper schema models:
  - User
  - Match
  - Message
  - CheckIn
- Data MUST persist across refreshes and restarts

2) AUTHENTICATION (REQUIRED)
- Implement Register + Login
- Hash passwords using bcrypt
- Issue JWT tokens on login
- Protect APIs with JWT middleware
- Frontend must send token in Authorization header

3) USER MODEL (FINAL)
User must include:
- id (UUID)
- name
- age
- gender (optional)
- email (unique)
- passwordHash
- gym
- goal (muscle / fatloss / fitness)
- experience (beginner / intermediate / advanced)
- preferredTime (morning / evening / night)
- consistencyScore (0–100)
- streak
- lastCheckIn
- createdAt

4) MATCHING ENGINE (NO FAKE 100%)
Implement weighted compatibility scoring:
- Same workout time → +40
- Same gym → +25
- Same goal → +20
- Same experience → +10
- Age difference ≤ 5 → +5

Rules:
- Calculate real percentage score
- Only return matches with score ≥ 70
- Sort matches by score DESC
- Return top 5 matches only
- Never match user with themselves

5) AI-STYLE CLUSTERING (EXPLAINABLE)
Implement deterministic habit-based clustering:
- Cluster ID based on:
  goal + preferredTime + experience
- Users in same cluster are marked "AI Recommended"
- Cluster ID must be consistent and explainable
- Add comments explaining why this qualifies as AI-style logic

6) CONSISTENCY & STREAK (REAL LOGIC)
- Allow one workout check-in per day
- If user checks in on consecutive days → streak++
- If user misses a day → streak resets
- Consistency score:
  +5 per valid check-in (max 100)
  -3 per missed day
- Consistency score must influence match ranking

7) CHAT SYSTEM (SAFE & REALISTIC)
- Chat allowed ONLY between valid matched users
- Messages stored in database
- Each message must include:
  senderId, matchId, content, timestamp
- Blocked users:
  - removed from matches
  - chat disabled
- Reported users flagged internally

8) BACKEND STRUCTURE (MANDATORY)
Organize backend as:
backend/
  server.js
  prisma/
    schema.prisma
    gymbuddy.db
  src/
    routes/
    controllers/
    services/
    middleware/
    utils/

Use clean separation of concerns.

9) FRONTEND INTEGRATION
- Keep existing UI
- Update frontend JS to call new APIs
- Handle auth token properly
- Show real scores (not always 100%)
- Show AI Recommended ONLY for same cluster

=============================
QUALITY & EVALUATION RULES
=============================
- NO in-memory arrays for core data
- NO hardcoded matches or users
- NO fake AI labels
- NO TODO comments
- Add comments explaining important logic
- App MUST run on Replit with `node server.js`

=============================
FINAL EXPECTATION
=============================
After refactor:
- Backend uses persistent database
- Users survive refresh
- Scores vary logically
- Clusters make sense
- Chat is gated & safe
- System is explainable in placements
- Architecture is scalable beyond Replit

This is a PLACEMENT-LEVEL PROJECT.
Do not simplify.
Do not compromise.
JUST IMPLEMENT EVERYTHING.
